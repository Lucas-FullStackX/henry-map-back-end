import { queries } from './queries';
import { mutations } from './mutations.js';
import { resolvers } from './resolvers.js';
import { types } from './types';

export const Category = { queries, mutations, resolvers, types };

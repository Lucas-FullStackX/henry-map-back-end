import { queries } from './queries';
import { mutations } from './mutations.js';
import { resolvers } from './resolvers.js';
import { types } from './types';

export const RoadMap = { queries, mutations, resolvers, types };
